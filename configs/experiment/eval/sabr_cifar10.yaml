# @package _global_

# to execute this experiment run:
# python src/eval.py experiment=eval/sabr_cifar10

defaults:
  - override /datamodule: cifar10.yaml
  - override /model: sabr_cifar10.yaml

model:
  init_method: null

# task name defines the output dir, e.g. "logs/${task_name}/runs/${now:date}"
task_name: "eval/sabr_cifar10"

# ckpt_path: "logs/weights/SABR/SABR_cifar10_8_best.pynet"

ckpt_path: ??
